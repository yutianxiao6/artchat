## 2026-06-26

- Inspected the OpenClaw/Flowdraw `通用短剧一键生成式工作流` (`video-short-drama`) implementation across `frontend/js/workflow/video-workflow.js`, `frontend/js/workflow/engine.js`, and `backend/api/workflow_router.py`.
- Created Codex skill `openclaw-short-drama-workflow` at `C:\Users\Administrator\.codex\skills\openclaw-short-drama-workflow`, with detailed process notes in `references/workflow-map.md`.
- User clarified they need a portable production-process skill, not a code-navigation skill. Created `short-drama-production-workflow`, focused on turning loose user video requirements into original stories and full AI short-drama production packages. Portable copy is in `skills/short-drama-production-workflow`.
- User asked why the skill had many files. Simplified `short-drama-production-workflow` into a single-file skill: only `SKILL.md` remains in both the Codex skills directory and the portable project copy.
- Investigated short-drama script generation failure `openai_error` / `bad_response`; patched `backend/api/workflow_router.py` so `call_llm_json` uses `build_endpoint_url` and retries without `response_format: json_object` when the upstream model/gateway rejects JSON mode.
- User said the portable short-drama production skill needed clearer Chinese production logic, modes, and per-step prompts. Rewrote `short-drama-production-workflow/SKILL.md` as a detailed single-file Chinese workflow with content modes, storyboard modes, step-by-step prompts, final package format, and prohibitions; validated both installed and portable copies.
