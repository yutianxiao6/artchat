## 2026-06-27

- Updated `short-drama-production-workflow` per user feedback: trigger on "start making a video" or direct video-making requests, extract prior context, ask only missing key info (theme, style, duration, aspect ratio), remove content-mode taxonomy, require storyboard mode confirmation, add user-specified mode guidance, clarify episode/segment rules, and embed code-aligned prompts from the OpenClaw workflow.
- Revised `short-drama-production-workflow` again to clarify dependencies: added global, per-segment, mode-tail, cross-segment dependency chains and a dependency checklist. Removed `minorCharacters` as an independent production step; `minor_characters` remains only as an optional segment data field in code-aligned JSON prompts.
- Split detailed prompts out of `short-drama-production-workflow/SKILL.md` into `references/prompts.md`. `SKILL.md` now keeps only the framework flow, dependency rules, mode guidance, output structure, and routing instructions for when to read the prompt reference.
