
[Sun 2026-04-26 20:17 GMT+8] Durable memory flush:
- Continued Android UI fixes in `python project/android-app/web/js/image.js` and `python project/android-app/web/js/chat.js` after user shifted priority from canvas image persistence to asset-library/chat UI issues.
- New user-reported issues to preserve:
  - asset library showed empty placeholder box when image address missing
  - asset long-press menu appeared on canvas layer / could not be tapped
  - chat bubble line spacing still too large
  - bottom chat input/composer was half hidden
  - chat message copy failed; later user requested removing normal-message copy buttons entirely and keeping copy button only for code blocks, with normal message copy handled by user long-press.
- Commits created during this phase:
  - `4a2b55f` — `android: persist canvas node images by asset id`
  - `5aa91b9` — `android: fix asset library empty state and compact chat ui`
  - `5c1838e` — `android: fix asset modal menu layer and tighten chat layout`
  - `c858cde` — `android: hide empty asset slots and improve chat copy`
  - `234da6a` — `android: restore asset actions and simplify chat copy ui`
  - `e5600f8` — `android: fix asset render crash and chat composer layout`
  - `9bda3c3` — `android: bump cache for asset crash fix`
- Cache-busting script versions advanced through:
  - `20260426al`
  - `20260426am`
  - `20260426an`
  - `20260426ao`
- Important direct fixes applied:
  - asset grid now intends to render only items with real displayable images (`visibleAssets` filtered from `filteredAssets`) instead of leaving empty visual card shells
  - asset context-menu buttons explicitly set to `type="button"`
  - normal chat bubble copy button removed from `renderMessageItem(...)`; code-block copy button remains via `[data-copy-code]`
  - `copyMsg(text)` hardened for Android WebView by using hidden fixed `textarea`, `focus()`, `select()`, `setSelectionRange(...)`, and checking `document.execCommand("copy")`
  - chat layout adjusted multiple times around `.oc-main`, `.oc-message-wrap`, `.oc-input-wrap`, `.oc-input-box`, `.oc-input` to solve Android bottom composer clipping / non-input behavior
- Important bug discovered via user logcat and fixed:
  - `File: https://localhost/js/image.js?v=20260426an - Line 1109 - Msg: Uncaught (in promise) ReferenceError: visibleAssets is not defined`
  - root cause: `renderLeftPanel()` referenced `visibleAssets` without defining it after refactor; fixed by restoring local definition:
    - `const categories = getAssetCategories(), filteredAssets = getFilteredAssets(), visibleAssets = filteredAssets.filter((item) => getDirectDisplayImageUrl(item.displayImageUrl) || getDirectDisplayImageUrl(item.imageUrl, item.imageBase64));`
- Current unresolved state at flush:
  - user still reported “素材库还是没反应” before the `visibleAssets` crash fix landed
  - chat bottom composer/input still reported broken: half hidden and unable to type text
  - likely next step if composer remains broken is structural refactor rather than more CSS nudges: detach input area from current flex/sticky flow, keep it fixed/absolute at bottom, and reserve explicit bottom space in message list
- Relevant exact identifiers touched this phase:
  - files:
    - `python project/android-app/web/js/image.js`
    - `python project/android-app/web/js/chat.js`
    - `python project/android-app/web/index.html`
  - functions/selectors:
    - `renderLeftPanel()`
    - `renderMessageItem(m, index)`
    - `copyMsg(text)`
    - `.oc-copy-btn`
    - `[data-copy-code]`
    - `.oc-main`
    - `.oc-message-wrap`
    - `.oc-input-wrap`
    - `.oc-input-box`
    - `.oc-input`
    - `[data-add-asset]`
    - `[data-move-asset]`
    - `[data-delete-asset]`
[Sun 2026-04-26 21:22 GMT+8] Android chat/file + UI follow-up durable notes:
- User reported and I fixed smart chat new-session payload bug where `/api/chat/smart` forwarded only `body.messages` and omitted current `body.message`, causing backend error `field messages is required (request id: B202604261253424568175018268d9d6uxxfHps0Master)` on fresh chats. Patch in `python project/android-app/web/js/mobile-api.js`; commits: `ec9ba21` (`android: include latest smart chat message in payload`) and cache bump `c657aa3` to `20260426au`.
- User then reported uploaded files only sent file names, not content. Improved file handling across `python project/android-app/web/js/chat.js` and `python project/android-app/web/js/mobile-api.js` so smart chat sends extracted text content and wraps attachments as `[附件内容开始: 文件名] ... [附件内容结束: 文件名]`. Commits: `c0a13bf` (`android: send extracted file text in smart chat`) and cache bump `d878d00` to `20260426av`.
- User preference update: do NOT show extracted character counts in UI, but DO perform length verification in logs; support only mainstream file formats and reject others instead of silently uploading unsupported files.
- Implemented/logged supported file validation in `python project/android-app/web/js/chat.js`: added `[chat][file] extracted text` and `[chat][file] collected smart files` console logs with `extractedLength`; restricted supported upload types to mainstream text/code formats plus `pdf/doc/docx`; unsupported types now throw `暂不支持该文件类型：...目前只支持 txt/md/json/csv/pdf/doc/docx 及常见代码文本文件。` Commits: `e8c3627` (`android: validate supported chat file types and log extraction`) and cache bump `becf14c` to `20260426aw`.
- Earlier same session UI work: assistant avatar resized to match user avatar; fold/collapse canvas drawer issue addressed via stronger mobile override in `python project/android-app/web/js/mobile-ui.js`; config page mobile layout heavily restyled in `python project/android-app/web/js/config.js`. Commits: `ab210da` (`android: redesign config mobile layout and sync avatar sizes`) and cache bump `e086c26` to `20260426at`.
- Ongoing constraint: Android work remains in `python project/android-app/`; user continues validating with `npx cap sync android` after each patch.
